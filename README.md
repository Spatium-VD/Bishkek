# Генератор сертификатов "Бишкек"

Веб-приложение для создания персонализированных сертификатов в формате PDF на основе шаблона.

## ✨ Возможности

- 📝 Ввод имени получателя, кода сертификата и текста поздравления
- 👁️ Предварительный просмотр текста на шаблоне
- 🖨️ Генерация и скачивание PDF-сертификата
- 🎨 Полная настройка позиций текста под любой шаблон

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/ваш-логин/Бишкек.git
cd Бишкек
```

### 2. Подготовка шаблона
1. Поместите ваш файл шаблона сертификата (PNG) в папку `assets/`
2. Переименуйте его в `sert.png` или обновите путь в `script.js`

### 3. Настройка координат текста
Откройте `script.js` и настройте параметры в разделе `CONFIG`:

```javascript
const CONFIG = {
    templateUrl: 'assets/sert.png', // Путь к шаблону
    
    // Координаты текста (x, y) в пикселях
    textPositions: {
        name: { x: 150, y: 250 },   // Позиция имени
        code: { x: 150, y: 310 },   // Позиция кода
        congrats: { x: 150, y: 370 } // Позиция поздравления
    },
    
    // Размеры шрифтов
    fontSize: {
        name: 44,     // Для имени
        code: 24,     // Для кода
        congrats: 20  // Для поздравления
    }
};
```

### 4. Запуск приложения
Откройте файл `index.html` в браузере или используйте Live Server.

## 📁 Структура проекта

```
Бишкек/
├── index.html              # Главная страница
├── style.css               # Стили
├── script.js               # Логика генерации PDF
├── assets/                 # Ресурсы
│   └── sert.png            # Шаблон сертификата
└── README.md               # Документация
```

## 🛠️ Технологии

- **HTML5** / **CSS3** для интерфейса
- **JavaScript (ES6+)** для логики
- **PDF-Lib** для работы с PDF
- **Canvas API** для предпросмотра

## 🔧 Настройка

### Подбор координат
1. Заполните форму тестовыми данными
2. Нажмите "Предпросмотр"
3. Если текст расположен неправильно, измените координаты в `script.js`
4. Повторяйте до получения нужного результата

### Использование своего шрифта
1. Загрузите TTF/OTF файл шрифта в папку `assets/fonts/`
2. Используйте функцию `embedFont` из PDF-Lib
3. Обновите настройки шрифта в `CONFIG`

## 🌐 Деплой

Приложение статическое, можно разместить на:
- GitHub Pages
- Netlify
- Vercel
- Любом хостинге статических файлов

## 📄 Лицензия

MIT
